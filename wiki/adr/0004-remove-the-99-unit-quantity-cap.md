# Remove the 99-unit quantity cap

User-entered and scanned quantities may be any positive whole number representable by the inventory
command model; Tridge no longer imposes an arbitrary 99-unit product limit. The reducer still
detects overflow and never wraps or silently clamps synchronized stock.
