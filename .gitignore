# SwiftPM
.build/
.swiftpm/

# Xcode
xcuserdata/
DerivedData/
*.xcuserstate

# Build output / fastlane (release lane writes the IPA + reports here)
build/
*.ipa
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output/

# Local-only receipt fixtures (may contain personal data)
Tests/ReceiptScanSmokeTests/Fixtures/private/

# Local secrets (copy env.sample → .env)
.env

# Worker (server/)
server/node_modules/
server/.wrangler/
server/.dev.vars
server/worker-configuration.d.ts
