# SwiftPM
.build/
.swiftpm/

# Xcode
xcuserdata/
DerivedData/
*.xcuserstate

# Local-only receipt fixtures (may contain personal data)
Tests/ReceiptScanSmokeTests/Fixtures/private/

# Local secrets (copy env.sample → .env)
.env

# App build secret (copy Secrets.sample.xcconfig → Secrets.xcconfig)
Secrets.xcconfig

# Worker (server/)
server/node_modules/
server/.wrangler/
server/.dev.vars
server/worker-configuration.d.ts
